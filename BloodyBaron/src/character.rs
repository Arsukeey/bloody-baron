use crate::map::Map;

pub struct Character {
    // pub name: String,
    // pub is_alive: bool,
    // pub is_killer: bool,
    // pub ability: fn(Vec<Character>, &Map) -> (),
    // pub dialogue: fn(&Character, Vec<Character>, &Map) -> ()
}

impl Character {
    pub fn freya() -> Self {
        let name = "Freya";
        let is_alive = true;

        Self {

        }
    }

    pub fn ravi() -> Self {
        let name = "Ravi";
        let is_alive = true;

        Self {
            
        }
    }

    pub fn luna() -> Self {
        let name = "Luna";
        let is_alive = true;

        Self {
            
        }
    }

    pub fn john() -> Self {
        let name = "John";
        let is_alive = true;

        Self {
            
        }
    }

    pub fn amanda() -> Self {
        let name = "Amanda";
        let is_alive = true;

        Self {
            
        }
    }

    pub fn vincent() -> Self {
        let name = "Vincent";
        let is_alive = true;

        Self {
            
        }
    }

    pub fn larissa() -> Self {
        let name = "Larissa";
        let is_alive = true;

        Self {
            
        }
    }

    pub fn chio() -> Self {
        let name = "Chio";
        let is_alive = true;

        Self {
            
        }
    }

    pub fn odette() -> Self {
        let name = "Odette";
        let is_alive = true;

        Self {
            
        }
    }
}